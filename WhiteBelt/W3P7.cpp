#include <iostream>
#include <vector>
#include <string>
#include <map>

using namespace std;

vector<string> Dupl(const vector<string>& s, const string& t){
    vector<string> temp;
    if(s.size() ==0 ){
        return s;
    }
    if(s.size() == 1 && s[0] == t){
        return t;
    }

    if(s.size() > 1){
        
    }
    



}

int main(int argc, char const *argv[])
{
    
    return 0;
}
